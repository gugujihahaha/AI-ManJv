# ============================================================
# 环境变量模板
#
# 本文件会提交到仓库，禁止填入任何真实密钥，只能写占位符。
#
# 用法：复制本文件为 .env，再填入真实值
#   Windows:     copy .env.example .env
#   macOS/Linux: cp .env.example .env
# .env 已被 .gitignore 忽略，不会进入版本库。
# ============================================================

# ----- 大语言模型：分镜拆分 / 角色卡 / 运镜参数（实验2起使用）-----
# 密钥申请：https://platform.deepseek.com
DEEPSEEK_API_KEY=sk-在此填入你的密钥

# ----- 以下接口实验3接入后再启用 -----
# IMAGE_API_KEY=   # 图像生成：逐格画面
# TTS_API_KEY=     # 语音合成：多角色配音
